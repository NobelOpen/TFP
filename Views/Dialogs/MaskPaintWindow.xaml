<Window x:Class="TaskFlow.Views.Dialogs.MaskPaintWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="掩膜画笔" 
        Height="700" Width="950"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource AppBackgroundBrush}"
        ResizeMode="CanResize"
        MinHeight="500" MinWidth="700">

    <Window.Resources>
        <Style x:Key="ToolButton" TargetType="ToggleButton">
            <Setter Property="Background" Value="{DynamicResource SurfaceHoverBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderLightBrush}"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="0,0,6,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource BorderDarkBrush}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource PrimaryButtonBgBrush}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource PrimaryButtonBgBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource PrimaryButtonTextBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Foreground" Value="{DynamicResource CreamyWhiteBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 工具栏 -->
        <Border Grid.Row="0" Background="{DynamicResource SurfaceBrush}" Padding="12,8" BorderBrush="{DynamicResource BorderLightBrush}" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <ToggleButton x:Name="BrushBtn" Content="🖌 画笔" Style="{StaticResource ToolButton}" IsChecked="True"
                              Checked="BrushBtn_Checked"/>
                <ToggleButton x:Name="EraserBtn" Content="🧹 橡皮擦" Style="{StaticResource ToolButton}"
                              Checked="EraserBtn_Checked"/>
                
                <Separator Margin="12,0" Background="{DynamicResource BorderLightBrush}"/>
                
                <TextBlock x:Name="TxtBrushSizeLabel" Text="画笔大小:" Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center" FontSize="12" Margin="0,0,8,0"/>
                <Slider x:Name="BrushSizeSlider" Width="120" Minimum="5" Maximum="80" Value="20" 
                        VerticalAlignment="Center" ValueChanged="BrushSizeSlider_ValueChanged"/>
                <TextBlock x:Name="BrushSizeText" Text="20" Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center" 
                           FontSize="12" Margin="8,0,0,0" MinWidth="25"/>
                
                <Separator Margin="12,0" Background="{DynamicResource BorderLightBrush}"/>
                
                <Button x:Name="BtnMaskAll" Content="掩膜全部" Background="{DynamicResource PrimaryButtonBgBrush}" Style="{StaticResource ActionButton}" 
                        Click="MaskAll_Click" Margin="0,0,8,0"/>
                <Button x:Name="BtnClearAll" Content="清除全部" Background="{DynamicResource DangerButtonBgBrush}" Style="{StaticResource ActionButton}" 
                        Click="ClearAll_Click" Margin="0,0,0,0"/>
            </StackPanel>
        </Border>

        <!-- 图像画布区域 -->
        <Grid Grid.Row="1" x:Name="ImageContainer" Background="{DynamicResource AppBackgroundBrush}" ClipToBounds="True"
              MouseLeftButtonDown="Canvas_MouseLeftButtonDown"
              MouseMove="Canvas_MouseMove"
              MouseLeftButtonUp="Canvas_MouseLeftButtonUp"
              MouseRightButtonDown="Canvas_MouseRightButtonDown"
              Cursor="Cross">
            <Image x:Name="SourceImage" Stretch="Uniform" 
                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <Canvas x:Name="OverlayCanvas" IsHitTestVisible="False"/>
        </Grid>

        <!-- 底部按钮栏 -->
        <Border Grid.Row="2" Background="{DynamicResource SurfaceBrush}" Padding="12,10" BorderBrush="{DynamicResource BorderLightBrush}" BorderThickness="0,1,0,0">
            <Grid>
                <TextBlock x:Name="TxtMaskHint" Text="左键涂抹遮蔽区域" 
                           Foreground="{DynamicResource TextTertiaryBrush}" FontSize="11" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="BtnReset" Content="重置" Background="{DynamicResource DangerButtonBgBrush}" 
                            Style="{StaticResource ActionButton}" Click="ResetButton_Click"/>
                    <Button x:Name="BtnComplete" Content="完成" Background="{DynamicResource PrimaryButtonBgBrush}" Foreground="{DynamicResource PrimaryButtonTextBrush}"
                            Style="{StaticResource ActionButton}" Click="CompleteButton_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
